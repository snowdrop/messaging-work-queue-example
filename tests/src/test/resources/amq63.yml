---
kind: Service
apiVersion: v1
spec:
  ports:
  - port: 5672
    targetPort: 5672
  selector:
    deploymentConfig: "upstate-amq"
metadata:
  name: "upstate-amq-amqp"
  labels:
    application: "upstate"
  annotations:
    description: The broker's AMQP port.
---
kind: Service
apiVersion: v1
spec:
  ports:
  - port: 1883
    targetPort: 1883
  selector:
    deploymentConfig: "upstate-amq"
metadata:
  name: "upstate-amq-mqtt"
  labels:
    application: "upstate"
  annotations:
    description: The broker's MQTT port.
---
kind: Service
apiVersion: v1
spec:
  ports:
  - port: 61613
    targetPort: 61613
  selector:
    deploymentConfig: "upstate-amq"
metadata:
  name: "upstate-amq-stomp"
  labels:
    application: "upstate"
  annotations:
    description: The broker's STOMP port.
---
kind: Service
apiVersion: v1
spec:
  ports:
  - port: 61616
    targetPort: 61616
  selector:
    deploymentConfig: "upstate-amq"
metadata:
  name: "upstate-amq-tcp"
  labels:
    application: "upstate"
  annotations:
    description: The broker's OpenWire port.
    service.alpha.openshift.io/dependencies: '[{"name": "upstate-amq-amqp",
      "kind": "Service"},{"name": "upstate-amq-mqtt", "kind": "Service"},{"name":
      "upstate-amq-stomp", "kind": "Service"}]'
---
kind: Service
apiVersion: v1
spec:
  clusterIP: None
  ports:
  - name: mesh
    port: 61616
  selector:
    deploymentConfig: "upstate-amq"
metadata:
  name: "upstate-amq-mesh"
  labels:
    application: "upstate"
  annotations:
    service.alpha.kubernetes.io/tolerate-unready-endpoints: 'true'
    description: Supports node discovery for mesh formation.
---
kind: DeploymentConfig
apiVersion: v1
metadata:
  name: "upstate-amq"
  labels:
    application: "upstate"
spec:
  strategy:
    type: Rolling
    rollingParams:
      maxSurge: 0
  triggers:
  - type: ImageChange
    imageChangeParams:
      automatic: true
      containerNames:
      - "upstate-amq"
      from:
        kind: ImageStreamTag
        name: jboss-amq-63:1.3
  - type: ConfigChange
  replicas: 1
  selector:
    deploymentConfig: "upstate-amq"
  template:
    metadata:
      name: "upstate-amq"
      labels:
        deploymentConfig: "upstate-amq"
        application: "upstate"
    spec:
      terminationGracePeriodSeconds: 60
      containers:
      - name: "upstate-amq"
        image: jboss-amq-63
        imagePullPolicy: Always
        readinessProbe:
          exec:
            command:
            - "/bin/bash"
            - "-c"
            - "/opt/amq/bin/readinessProbe.sh"
        ports:
        - name: jolokia
          containerPort: 8778
          protocol: TCP
        - name: amqp
          containerPort: 5672
          protocol: TCP
        - name: mqtt
          containerPort: 1883
          protocol: TCP
        - name: stomp
          containerPort: 61613
          protocol: TCP
        - name: tcp
          containerPort: 61616
          protocol: TCP
        env:
        - name: AMQ_USER
          value: "upstate"
        - name: AMQ_PASSWORD
          value: "upstate"
        - name: AMQ_TRANSPORTS
          value: "amqp"
        - name: AMQ_QUEUES
          value: "upstate/requests,upstate/response,upstate/worker-status"
        - name: AMQ_TOPICS
          value: ""
        - name: MQ_SERIALIZABLE_PACKAGES
          value: ""
        - name: AMQ_MESH_DISCOVERY_TYPE
          value: "dns"
        - name: AMQ_MESH_SERVICE_NAME
          value: "upstate-amq-mesh"
        - name: AMQ_MESH_SERVICE_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: AMQ_STORAGE_USAGE_LIMIT
          value: "100 gb"
        - name: AMQ_QUEUE_MEMORY_LIMIT
          value: ""
